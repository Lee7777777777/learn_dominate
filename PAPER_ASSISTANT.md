# 论文学习助手

主界面点击 **从论文生成地图**。生成结果作为草稿预览，保存时创建独立新地图，不覆盖已有地图。

## Codex 自动分析

1. 安装并登录 Codex CLI。知路自动查找 `codex.exe`，也可手动选择可执行文件。
2. 选择 PDF、学习目标、已有基础和地图规模。
3. 勾选允许将内容交给 Codex，然后点击 **用 Codex 分析**。
4. 在地图预览中滚动并点击节点查看解释、目标、任务与来源。可在 **粘贴结果 / 高级编辑** 中调整 JSON 节点和关系，点击 **校验并刷新预览**。
5. 填写地图名称，点击 **保存为新地图**。保存后继续使用已有内容编辑器、颜色设置和进度管理。

这是独立 Codex 任务，不继承桌面里正在进行的聊天。复用 CLI 登录与配置，消耗相应账号额度；API 登录按其计费规则处理。Agent 使用只读沙箱，本应用从最终结果创建地图。后台运行不显示 PowerShell。Windows 自动沿用已有 HTTP/HTTPS 代理，不修改系统设置。

点击 **停止分析** 会终止本次 Codex 进程树；PDF 解析会在当前页处理结束后停止。分析超过 30 分钟自动停止。失败不覆盖上次成功的 `result.json`，打开同一任务包可重试；任务目录中的 `codex.log` 用于检查登录、额度或网络错误。

## 豆包工作等外部 Agent

1. 选择论文和学习设置，点击 **导出外部 Agent 任务包**，选择保存位置。
2. 应用创建独立的 `paper-任务编号` 目录。把论文、`分析要求.txt`、`manifest.json`、`result.schema.json` 和文字材料交给 Agent。
3. 要求 Agent 按分析要求生成 `result.json`。若它只能返回文字，复制完整 JSON 即可。
4. 回到知路打开对应任务包，在 **地图草稿** 中导入结果文件，或粘贴 JSON 并校验。

此模式不自动控制豆包工作界面，也不假定它提供开放调用接口。只使用它实际支持的文件上传、结果保存或文本复制能力。

## 来源与限制

- 本版支持可提取文字的 PDF：50 MB 以内、1～300 页、最多 16 万字符；超限会明确报错，不静默截断。
- 暂不包含 OCR、图片理解或公式专用识别。扫描版先做 OCR；双栏顺序、公式和表格仍可能提取不准确，重要内容需对照原论文。
- 每个“论文内容”节点必须附 PDF **物理页码**和 8～300 字符摘录。“AI建议”包括补充基础、练习和学习安排。
- 核对页码范围和摘录文字；无法匹配的摘录保留“待核对”标记。文字匹配不代表分析结论正确。
- 检查任务编号、论文 SHA256、重复节点、无效连线、自连接和循环前置依赖。每份结果最多 60 个节点、180 条关系、2 MB。
- 地图草稿支持 JSON 编辑；第一版不自动合并已有地图，也不提供图形化草稿连线编辑。保存后使用正常地图编辑功能。

## 本地文件与隐私

自动任务保存在学习数据目录下的 `paper_tasks/`。打包版为 `%LOCALAPPDATA%\ZhiLu\data\paper_tasks/`；外部任务保存到你选择的目录。任务包包含论文副本、文字、学习要求和结果，日志也可能包含论文内容。关闭应用后可自行备份或删除不需要的任务目录；地图节点中的笔记与来源摘录仍保存在地图数据库中。

“本地 Agent 接入”不等于离线推理：使用云端模型时，论文材料会发送给对应服务商。知路不读取或复制 Codex 登录凭据，不把密钥打包到 EXE。用户论文和任务目录不随项目发布。

源码启动先安装 `requirements.txt`。Codex 官方入口：[非交互模式](https://learn.chatgpt.com/docs/non-interactive-mode)、[认证](https://learn.chatgpt.com/docs/auth)。PDF 解析：[pypdf 文档](https://pypdf.readthedocs.io/en/stable/user/extract-text.html)。
